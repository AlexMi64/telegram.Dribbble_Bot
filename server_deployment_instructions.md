# 🚀 Руководство по развертыванию DesignLike Bot на сервере

## 📋 Перед началом

### Требования к серверу
- **ОС**: Ubuntu/Debian/Linux
- **RAM**: Минимум 512MB (рекомендовано 1GB+)
- **Диск**: Минимум 1GB свободного места
- **Node.js**: 18+
- **PM2**: Для управления процессами

### Предварительная подготовка
```bash
# Установка Node.js 18+
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# Установка PM2 глобально
sudo npm install -g pm2

# Проверка установки
node --version  # Должен выводить v18+
npm --version   # Должен работать
pm2 --version   # Должен работать
```

## 🛠️ Процесс развертывания

### 1. Клонирование репозитория
```bash
cd /home/ubuntu/  # или другая директория
git clone https://github.com/AlexMi64/telegram.Dribbble_Bot.git
cd telegram.Dribbble_Bot
```

### 2. Установка зависимостей
```bash
npm install
```

### 3. Настройка переменных окружения
```bash
# Создайте файл .env на основе шаблона
cp .env.example .env

# Отредактируйте .env с реальными токенами
nano .env
```

**Обязательные переменные:**
```env
BOT_TOKEN=8219160863:AAF8smrlNw9txg0kFdqC4nRurGlxozPXJbE
ADMIN_ID=366323850
BOT_USERNAME=@LikeDesignBot
NODE_ENV=production
```

### 4. 🌟 АВТОМАТИЧЕСКОЕ РАЗВЕРТЫВАНИЕ (РЕКОМЕНДОВАНО)
```bash
# Запустите скрипт развертывания - он безопасно обновит базу данных
npm run deploy
```

**Что делает скрипт deploy:**
- ✅ Проверяет существующую базу данных
- ✅ Создает резервную копию если база существует
- ✅ Обновляет схему без потери данных
- ✅ Создает новые таблицы (completed_url_actions)
- ✅ Добавляет индексы для производительности
- ✅ Проверяет целостность данных
- ✅ Выводит статистику после миграции

### 5. Запуск бота через PM2
```bash
# Запуск в фоне
npm run pm2:start

# Проверка статуса
pm2 status

# Просмотр логов
pm2 logs designlike-bot

# Перезапуск при обновлениях
npm run pm2:restart
```

## 🔒 БЕЗОПАСНОСТЬ ДАННЫХ

### Гарантии скрипта deploy:
- **Автоматическое резервное копирование**: База копируется перед изменениями
- **Восстановление при ошибке**: При проблемах база автоматически восстанавливается
- **Сохранение всех данных**: Пользователи, проекты, действия - все остается нетронутым
- **Миграция без потери**: Новые таблицы добавляются, существующие обновляются

### Резервные копии:
```
bot.db.backup.TIMESTAMP  # Автоматические бэкапы
```

## 🎯 После развертывания

### Проверка работоспособности:
```bash
# Проверка PM2
pm2 status
pm2 logs designlike-bot

# Если бот не отвечает - перезапустить
npm run pm2:restart
```

### Статистика бота:
```bash
# Подключитесь к базе данных SQLite
sqlite3 bot.db

# Проверка данных
SELECT 'Пользователей' as type, COUNT(*) as count FROM users;
SELECT 'Проектов' as type, COUNT(*) as count FROM projects;
SELECT 'Действий' as type, COUNT(*) as count FROM action_transactions;
.quit
```

## 🔄 ОБНОВЛЕНИЕ БОТА НА ПРОДАКШЕН

### При выходе новой версии:
```bash
# Остановить бота
npm run pm2:stop

# Обновить код
git pull origin main

# Установить новые зависимости (если есть)
npm install

# Запустить миграцию базы (безопасно)
npm run deploy

# Запустить бота
npm run pm2:start

# Проверить логи
pm2 logs designlike-bot
```

## 🚨 Устранение неполадок

### Бот не стартует:
```bash
# Проверить переменные окружения
cat .env

# Проверить логи
npm run pm2:restart
pm2 logs designlike-bot --lines 50
```

### База данных повреждена:
```bash
# Восстановление из последнего бэкапа
ls -la bot.db.backup.*
cp bot.db.backup.LAST_TIMESTAMP bot.db
npm run pm2:restart
```

### Ошибки миграции:
```bash
# Ручной запуск скрипта миграции
node deploy_to_server.js

# Или принудительное восстановление
npm run clear-db  # ⚠️ УДАЛИТ ВСЕ ДАННЫЕ!
```

## 📊 Мониторинг

### PM2 мониторинг:
```bash
pm2 status          # Статус процессов
pm2 monit          # Реал-тайм мониторинг
pm2 logs designlike-bot --lines 100  # Последние логи
```

### Автоматический перезапуск:
PM2 автоматически перезапускает бота при падениях.

## 📞 Поддержка

При проблемах с развертыванием:
- Проверьте логи: `pm2 logs designlike-bot`
- Убедитесь что все переменные окружения установлены
- Проверьте доступность интернета
- Убедитесь что токен бота действующий

## ✅ Список проверок после развертывания:
- [ ] Бот отвечает на `/start`
- [ ] Админ панель работает (`/admin`)
- [ ] Пользователи могут регистрироваться
- [ ] Система заданий функционирует
- [ ] Данные пользователей сохранены
- [ ] Реферальная система работает
- [ ] PM2 показывает статус "online"
